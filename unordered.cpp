#include <iostream>
#include<unordered_set>
using namespace std;

int main()
{
    // Same As Set But Stores The Element In Unorederd Way
    unordered_set<int> us;
    us.insert(10);
    us.insert(1);
    us.insert(2);
    return 0;
}